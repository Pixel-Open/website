{% extends '_default/page.html.twig' %}

            {%- block content ~%}
            <h1>{{ page.title|e }}</h1>
            <div>
              <time>{{ page.date|format_date('long') }}</time> &middot; {% include 'partials/terms-list.html.twig' with {'page': page, 'vocabulary': 'tags'} only %}
            </div>
                {{ page.content }}
            {%- if page.prev is defined or page.next is defined ~%}
            <nav>
              <ul>
                {%- if page.prev is defined ~%}
                <li><a href="{{ url(page.prev) }}">{{ page.prev.title }}</a></li>
                {%- endif ~%}
                {%- if page.next is defined ~%}
                <li><a href="{{ url(page.next) }}">{{ page.next.title }}</a></li>
                {%- endif ~%}
              </ul>
            </nav>
            {%- endif ~%}
            {%- endblock content ~%}
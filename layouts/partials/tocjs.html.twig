
<script
  src="{{ asset('https://cdnjs.cloudflare.com/ajax/libs/tocbot/' ~ site.tocbot.version ~ '/tocbot.min.js', {minify: false}) }}"></script>
<script>{% apply minify_js %}
  tocbot.init({
    tocSelector: '.toc',
    contentSelector: '#content',
    headingSelector: 'h2, h3, h4',
    hasInnerContainers: true,
    scrollSmooth: true,
    headingsOffset: 100,
    scrollSmoothOffset: -100
  });
  {% endapply %}</script>